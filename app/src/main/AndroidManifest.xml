<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sam.pizza">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomerActivities.MakeOrderCustomerActivity_old" />
        <activity android:name=".OwnerActivities.OwnerActivity" />
        <activity android:name=".DeliveryActivities.DeliveryListActivity" />
        <activity android:name=".CustomerActivities.CustomerActivity" />
        <activity android:name=".CustomerActivities.CheckOrderActivity" />
        <activity android:name=".CustomerActivities.FeedbackActivity" />
        <activity android:name=".CustomerActivities.MakeOrderCustomerActivity" />
        <activity android:name=".CustomerActivities.ConfirmOrderActivity" />
        <activity android:name=".DeliveryActivities.FinishedOrderedActivity" />
        <activity android:name=".DeliveryActivities.OrderInfoActivity"></activity>
    </application>

</manifest>