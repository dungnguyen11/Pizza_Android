<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sam.pizza">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Customer.MakeOrderCustomerActivity_old" />
        <activity android:name=".Owner.OwnerActivity" />
        <activity android:name=".Delivery.DeliveryListActivity" />
        <activity android:name=".Customer.CustomerActivity" />
        <activity android:name=".Customer.CheckOrderActivity" />
        <activity android:name=".Customer.FeedbackActivity" />
        <activity android:name=".Customer.MakeOrderCustomerActivity" />
        <activity android:name=".Customer.ConfirmOrderActivity" />
        <activity android:name=".Delivery.FinishedOrderedActivity" />
        <activity android:name=".Delivery.OrderInfoActivity"></activity>
    </application>

</manifest>